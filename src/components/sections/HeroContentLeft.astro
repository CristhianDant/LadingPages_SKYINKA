<div class="hero-left">
  <img src="/images/APERTURA.webp" alt="Salto tándem en caída libre" class="hero-image" />
  <div class="hero-overlay"></div>
  <div class="hero-content-left">
    <h1 class="hero-title">Salto<br />Tándem<br />Safiro</h1>
    <div class="hero-price-block">
      <span class="price-label">Desde</span>
      <span class="price-value">S/. 1,970 <span class="price-igv">+ IGV</span></span>
    </div>
    <div class="hero-buttons">
      <a href="https://skipperu.odoo.com/shop/tdms-salto-tandem-safiro-3" class="btn-reservar" target="_blank" rel="noopener">
        Reservar Ahora
      </a>
      <a href="#experiencia" class="btn-explorar">
        Explorar
      </a>
    </div>
  </div>
</div>

<style>
  .hero-left {
    position: relative;
    overflow: hidden;
    height: 100%;
  }

  .hero-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(0, 0, 0, 0.2) 0%,
      rgba(0, 0, 0, 0.6) 100%
    );
  }

  .hero-content-left {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 60px;
    z-index: 2;
  }

  .hero-title {
    font-family: var(--font-heading);
    font-size: clamp(48px, 6vw, 80px);
    line-height: 0.95;
    text-transform: uppercase;
    color: var(--text-primary);
    font-weight: 400;
    margin-bottom: 32px;
  }

  .hero-price-block {
    display: flex;
    flex-direction: column;
    gap: 4px;
    margin-bottom: 32px;
  }

  .price-label {
    font-family: var(--font-body);
    font-size: 14px;
    color: var(--text-secondary);
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .price-value {
    font-family: var(--font-heading);
    font-size: 48px;
    color: var(--text-primary);
    line-height: 1;
  }

  .price-igv {
    font-size: 24px;
    color: var(--text-secondary);
  }

  .hero-buttons {
    display: flex;
    gap: 16px;
  }

  .btn-reservar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 16px 32px;
    background-color: var(--primary-blue);
    color: var(--text-primary);
    font-family: var(--font-header);
    font-size: 14px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    transition: var(--transition);
    text-decoration: none;
  }

  .btn-reservar:hover {
    background-color: var(--primary-dark);
  }

  .btn-explorar {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 16px 32px;
    background-color: transparent;
    color: var(--text-primary);
    font-family: var(--font-header);
    font-size: 14px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    border: 2px solid var(--text-primary);
    transition: var(--transition);
    text-decoration: none;
  }

  .btn-explorar:hover {
    background-color: var(--text-primary);
    color: var(--bg-dark);
  }

  @media (max-width: 1024px) {
    .hero-left {
      height: 70vh;
      min-height: 600px;
    }

    .hero-content-left {
      padding: 40px;
    }
  }

  @media (max-width: 768px) {
    .hero-left {
      height: 60vh;
      min-height: 500px;
    }

    .hero-content-left {
      padding: 30px;
    }

    .hero-title {
      font-size: 42px;
    }

    .price-value {
      font-size: 36px;
    }

    .hero-buttons {
      flex-direction: column;
    }
  }
</style>
